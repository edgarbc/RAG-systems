# Core dependencies for RAG demo
# Lightweight set - no heavy native deps like faiss-cpu or torch

transformers>=4.0.0
sentence-transformers>=2.0.0
rank_bm25>=0.2.2
tqdm>=4.0.0
numpy>=1.21.0
scikit-learn>=1.0.0
